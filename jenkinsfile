pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                script {
                    sh './gradlew dependencies'
                    sh './gradlew build --stacktrace'
                    sh './gradlew test'
                }
            }
        }
    }
}
