pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                script {
                    sh 'chmod 777 ./gradlew'
                    sh './gradlew dependencies'
                    sh './gradlew build --stacktrace'
                    sh './gradlew test'
                }
            }
        }
    }
}
