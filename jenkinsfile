pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                script {
                    sh 'echo "hello"'
                }
            }
        }
    }
}
